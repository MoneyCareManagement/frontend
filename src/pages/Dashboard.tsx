import BalanceCard from "../components/BalanceCard";

export default function Dashboard() {
    return (
        <main className="bg-purple-900 w-screen h-screen flex flex-col items-center">
            <BalanceCard/>
        </main>
    );
};